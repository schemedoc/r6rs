Formal comment #158 (defect)

Typo - 5.92 ("max" should be "may")
Reported by: 	Peter Wright
Version:	5.92

Section 5.1 - Requirement levels

Both in the definitions of "should" and "should not", the phrase
"valid reasons max exist" is used - the "max" should (I assume) be
"may".
